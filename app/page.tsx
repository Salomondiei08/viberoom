"use client";

import { useEffect, useMemo, useState } from "react";
import {
  ArrowUpRight, Bell, Check, ChevronDown, CircleHelp, Clock3, Command,
  FileText, Filter, Inbox, LayoutGrid, Menu, MessageCircle, MoreHorizontal,
  Play, Search, Send, Settings2, Sparkles, Star, Tag, Users, X,
} from "lucide-react";
import DashboardFr from "./dashboard";
import VibePage from "./vibe-page";

export type Status = "Nouveau" | "Sélectionné" | "À étudier" | "New" | "Shortlisted" | "Review";
export type Application = { id: number; name: string; handle: string; location: string; focus: string; bio: string; status: Status; color: string; initials: string; time: string };

const initialApplications: Application[] = [];

const statusStyles: Record<Status, string> = { Nouveau: "status-new", Sélectionné: "status-shortlisted", "À étudier": "status-review", New: "status-new", Shortlisted: "status-shortlisted", Review: "status-review" };

const Logo = () => <div className="brand-lockup"><span className="brand-mark"><span /></span><span>LIVE<span className="brand-accent">ROOM</span></span></div>;

const Avatar = ({ application, small = false }: { application: Application; small?: boolean }) => <div className={`avatar ${small ? "avatar-small" : ""}`} style={{ background: application.color }}>{application.initials}</div>;

export default function Home() {
  const [view, setView] = useState<"landing" | "dashboard">("landing");
  const [applications, setApplications] = useState(initialApplications);
  const [selectedId, setSelectedId] = useState(1);
  const [filter, setFilter] = useState<"Toutes" | Status>("Toutes");
  const [menuOpen, setMenuOpen] = useState(false);
  const [submitted, setSubmitted] = useState(false);

  useEffect(() => { fetch("/api/applications").then((response) => response.json()).then((data: Application[] | { error?: string }) => { if (!Array.isArray(data)) return; setApplications(data); if (data[0]) setSelectedId(data[0].id); }).catch(() => undefined); }, []);
  const selected = applications.find((item) => item.id === selectedId) ?? applications[0];
  const visibleApplications = useMemo(() => filter === "Toutes" ? applications : applications.filter((item) => item.status === filter), [applications, filter]);

  const submitApplication = (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const form = new FormData(event.currentTarget);
    const name = String(form.get("name") || "New applicant");
    const payload = { name, location: String(form.get("location") || "Afrique de l’Ouest"), focus: String(form.get("focus") || "Mon histoire"), bio: String(form.get("story") || "") };
    fetch("/api/applications", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }).then((response) => response.json()).then((next: Application) => { setApplications((current) => [next, ...current]); setSelectedId(next.id); setSubmitted(true); event.currentTarget.reset(); }).catch(() => undefined);
  };

  const updateStatus = (status: Status) => { if (!selected) return; setApplications((current) => current.map((item) => item.id === selected.id ? { ...item, status } : item)); fetch(`/api/applications/${selected.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status }) }).catch(() => undefined); };

  return <VibePage />;

  if (view === "dashboard" && selected) return <DashboardFr applications={applications} selected={selected} visibleApplications={visibleApplications} selectedId={selectedId} setSelectedId={setSelectedId} filter={filter} setFilter={setFilter} updateStatus={updateStatus} setView={setView} menuOpen={menuOpen} setMenuOpen={setMenuOpen} />;

  return (
    <main className="site-shell">
      <header className="topbar"><Logo /><nav className="desktop-nav"><a href="#why">Le concept</a><a href="#how">Comment ça marche</a><a href="#faq">FAQ</a></nav><div className="topbar-actions"><button className="text-button desktop-only" onClick={() => setView("dashboard")}>Ouvrir le tableau de bord <ArrowUpRight size={15} /></button><button className="icon-button mobile-menu" aria-label="Ouvrir le menu" onClick={() => setMenuOpen(!menuOpen)}><Menu size={20} /></button><button className="button button-dark desktop-only" onClick={() => document.getElementById("apply")?.scrollIntoView({ behavior: "smooth" })}>Candidater <ArrowUpRight size={16} /></button></div></header>
      {menuOpen && <div className="mobile-nav"><a href="#why" onClick={() => setMenuOpen(false)}>Le concept</a><a href="#how" onClick={() => setMenuOpen(false)}>Comment ça marche</a><a href="#faq" onClick={() => setMenuOpen(false)}>FAQ</a><button onClick={() => setView("dashboard")}>Ouvrir le tableau de bord</button></div>}

      <section className="hero section-pad"><div className="hero-copy"><div className="eyebrow"><span className="live-dot" />Candidatures ouvertes · Saison 01</div><h1>Ton histoire<br /><em>mérite</em> le live.</h1><p className="hero-description">Une conversation vraie avec celles et ceux qui construisent, créent et changent la suite. Pas besoin de présentation parfaite.</p><div className="hero-actions"><button className="button button-coral" onClick={() => document.getElementById("apply")?.scrollIntoView({ behavior: "smooth" })}>Raconter mon histoire <ArrowUpRight size={17} /></button><button className="watch-link" onClick={() => document.getElementById("why")?.scrollIntoView({ behavior: "smooth" })}><span className="play-circle"><Play size={12} fill="currentColor" /></span> Voir le concept</button></div><div className="hero-proof"><div className="avatar-stack"><span style={{ background: "#ed7f65" }}>AM</span><span style={{ background: "#6864ba" }}>KM</span><span style={{ background: "#d79a48" }}>YK</span><span style={{ background: "#8a5a75" }}>+</span></div><span>Déjà <strong>128</strong> personnes dans la salle</span></div></div><div className="hero-art"><div className="art-orbit orbit-one" /><div className="art-orbit orbit-two" /><div className="art-glow" /><div className="portrait-card"><div className="portrait-top"><span className="recording"><i /> EN DIRECT</span><span>01 / 04</span></div><div className="portrait-image"><div className="portrait-figure"><div className="portrait-head" /><div className="portrait-body" /></div><div className="portrait-spark">✦</div></div><div className="portrait-caption"><span>LA SALLE</span><strong>Créer quelque chose<br />qui compte.</strong></div></div><div className="floating-note note-one"><Sparkles size={14} /><span><b>Bonne énergie.</b><br />C’est exactement ça.</span></div><div className="floating-note note-two"><MessageCircle size={15} /><span>« La version<br />la plus vraie. »</span></div><div className="art-label">L/01 <span>Histoires en mouvement</span></div></div></section>

      <div className="tiktok-banner"><span>♪</span> LiveRoom est connecté à ton univers TikTok — pour faire passer ton histoire du fil au direct.</div>
      <div className="ticker"><div>POUR LES CURIEUX ✦ LES COURAGEUX ✦ LES IMPARFAITS ✦ CEUX QUI ONT COMMENCÉ QUAND MÊME ✦ POUR LES CURIEUX ✦ LES COURAGEUX ✦</div></div>

      <section id="why" className="manifesto section-pad"><div className="section-kicker">01 — LA SALLE</div><div className="manifesto-grid"><h2>Moins de<br /><em>performance.</em><br />Plus de vrai.</h2><div className="manifesto-copy"><p>LiveRoom est une série de conversations en direct pour les personnes qui ont quelque chose de vrai à partager. Nous cherchons les créateurs, les audacieux et les personnes qui pensent autrement.</p><p>Chaque épisode permet d’être vu tel que l’on est — pas résumé par un CV.</p><a href="#apply" className="arrow-link">Qu’est-ce qu’une bonne histoire ? <ArrowUpRight size={16} /></a></div></div></section>

      <section id="how" className="how-section section-pad"><div className="section-kicker">02 — SIMPLE ET CLAIR</div><div className="section-heading"><h2>De l’idée<br />au <em>direct.</em></h2><p>De bonnes questions. Une salle bienveillante.<br />Pas d’audition, juste une vraie conversation.</p></div><div className="steps"><div className="step-card step-featured"><span className="step-number">01</span><div className="step-icon"><FileText size={24} /></div><h3>Dis-nous ce<br />que tu fais</h3><p>Quelques lignes suffisent. Nous cherchons l’étincelle, pas la perfection.</p><ArrowUpRight className="step-arrow" size={20} /></div><div className="step-card"><span className="step-number">02</span><div className="step-icon"><MessageCircle size={24} /></div><h3>On apprend<br />à te connaître</h3><p>Si ton histoire correspond, nous te contacterons pour échanger.</p></div><div className="step-card"><span className="step-number">03</span><div className="step-icon"><Play size={24} /></div><h3>Rejoins la<br />conversation</h3><p>Viens comme tu es. On s’occupe du reste.</p></div></div></section>

      <section id="apply" className="apply-section section-pad"><div className="apply-intro"><div className="section-kicker">03 — À TON TOUR</div><h2>Apporte le<br /><em>vrai.</em></h2><p>Pas besoin de trop réfléchir. Dis-nous ce que tu construis, apprends ou regardes différemment.</p><div className="apply-note"><Star size={16} fill="currentColor" /><span>Il n’y a pas de réponse parfaite.<br /><strong>Il y a ta réponse.</strong></span></div></div><form className="application-form" onSubmit={submitApplication}><div className="form-header"><span>Candidature / 01</span><span>~ 3 min</span></div><label>Ton nom<input required name="name" placeholder="ex. Mariam Koné" /></label><div className="form-row"><label>Où habites-tu ?<input required name="location" placeholder="Ville, pays" /></label><label>Quel est ton sujet ?<input required name="focus" placeholder="ex. Entreprendre en public" /></label></div><label>De quoi veux-tu parler ?<textarea required name="story" rows={4} placeholder="La chose à laquelle tu reviens toujours..." /></label><label className="consent"><input required type="checkbox" /> <span>J’accepte que l’équipe LiveRoom me contacte au sujet de cette conversation.</span></label><button className="button button-coral submit-button" type="submit">Envoyer ma candidature <Send size={16} /></button>{submitted && <div className="success-message"><Check size={17} /> C’est envoyé. Nous revenons vers toi bientôt.</div>}</form></section>

      <footer className="footer section-pad"><Logo /><span>Made for the people making things.</span><span>© 2026 LiveRoom</span></footer>
    </main>
  );
}

function Dashboard({ applications, selected, visibleApplications, selectedId, setSelectedId, filter, setFilter, updateStatus, setView, menuOpen, setMenuOpen }: { applications: Application[]; selected: Application; visibleApplications: Application[]; selectedId: number; setSelectedId: (id: number) => void; filter: "All" | Status; setFilter: (filter: "All" | Status) => void; updateStatus: (status: Status) => void; setView: (view: "landing" | "dashboard") => void; menuOpen: boolean; setMenuOpen: (open: boolean) => void }) {
  return <main className="dashboard-shell"><aside className={`sidebar ${menuOpen ? "sidebar-open" : ""}`}><div className="sidebar-header"><Logo /><button className="icon-button mobile-menu" onClick={() => setMenuOpen(false)}><X size={18} /></button></div><div className="workspace-switcher"><div className="workspace-avatar">L</div><div><strong>LiveRoom</strong><span>Season 01</span></div><ChevronDown size={16} /></div><div className="side-label">Workspace</div><nav className="side-nav"><button className="active"><Inbox size={17} /> Applications <span>{applications.length}</span></button><button><Users size={17} /> Guests <span>12</span></button><button><FileText size={17} /> Episodes</button></nav><div className="side-label">Manage</div><nav className="side-nav"><button><Tag size={17} /> Topics</button><button><Settings2 size={17} /> Settings</button></nav><div className="sidebar-bottom"><div className="help-card"><CircleHelp size={18} /><div><strong>Need a hand?</strong><span>Read the host guide</span></div></div><button className="profile-row"><div className="profile-avatar">SK</div><span>Salomon K.</span><MoreHorizontal size={17} /></button></div></aside><div className="dashboard-content"><header className="dashboard-header"><button className="icon-button dashboard-menu" onClick={() => setMenuOpen(true)}><Menu size={20} /></button><div className="dashboard-title"><span className="breadcrumb">Workspace / Applications</span><h1>Applications <span>{applications.length}</span></h1></div><div className="dashboard-actions"><button className="icon-button"><Search size={18} /></button><button className="icon-button"><Bell size={18} /></button><button className="button button-dark" onClick={() => setView("landing")}>View public page <ArrowUpRight size={15} /></button></div></header><div className="dashboard-main"><div className="stats-row"><div className="stat-card"><span>Total applications</span><strong>{applications.length}</strong><small className="up">↑ 18% <i>vs last week</i></small></div><div className="stat-card"><span>New this week</span><strong>{applications.filter((item) => item.status === "New").length}</strong><small className="up">↑ 4 <i>vs last week</i></small></div><div className="stat-card stat-highlight"><span>Season capacity</span><strong>64<span>/100</span></strong><div className="capacity-bar"><i /></div><small>36 spots left</small></div><div className="pulse-card"><div className="pulse-orb" /><div><span>LiveRoom pulse</span><strong>Good stories<br />are coming in.</strong></div><Sparkles size={20} /></div></div><div className="review-layout"><section className="application-list"><div className="list-toolbar"><div className="filter-tabs">{(["All", "New", "Shortlisted", "Review"] as const).map((item) => <button key={item} className={filter === item ? "selected" : ""} onClick={() => setFilter(item)}>{item}{item !== "All" && <span>{applications.filter((application) => application.status === item).length}</span>}</button>)}</div><button className="filter-button"><Filter size={15} /> Filters</button></div><div className="list-head"><span>{visibleApplications.length} applications</span><span>Newest first <ChevronDown size={14} /></span></div><div className="application-items">{visibleApplications.map((application) => <button key={application.id} className={`application-item ${application.id === selectedId ? "item-selected" : ""}`} onClick={() => setSelectedId(application.id)}><Avatar application={application} /><div className="applicant-summary"><strong>{application.name}</strong><span>{application.handle} · {application.location}</span></div><span className={`status-pill ${statusStyles[application.status]}`}>{application.status}</span><span className="item-time">{application.time}</span></button>)}</div></section><section className="detail-panel"><div className="detail-top"><span className="detail-label">Application detail</span><button className="icon-button"><MoreHorizontal size={19} /></button></div><div className="detail-person"><Avatar application={selected} /><div><h2>{selected.name}</h2><span>{selected.handle} · {selected.location}</span></div><button className="star-button"><Star size={18} /></button></div><div className="detail-tags"><span>{selected.focus}</span><span className={`status-pill ${statusStyles[selected.status]}`}>{selected.status}</span></div><div className="detail-divider" /><div className="detail-section"><span className="detail-label">Their story</span><p>{selected.bio}</p></div><div className="detail-section"><span className="detail-label">Host notes</span><div className="notes-box">Add a note about this applicant...<button className="icon-button"><Send size={14} /></button></div></div><div className="detail-footer"><span>Applied {selected.time}</span><div className="status-actions"><span>Move to</span><button onClick={() => updateStatus("Review")} className={selected.status === "Review" ? "current" : ""}>Review</button><button onClick={() => updateStatus("Shortlisted")} className={selected.status === "Shortlisted" ? "current" : ""}>Shortlist</button></div></div></section></div></div></div></main>;
}
